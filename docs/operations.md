# Operations
Monitor the minimal health endpoint, safe error codes, latency, fallbacks, and landing errors without bodies/tokens/comments. Rotate a device by revoking then issuing a once-visible credential. Rotate token keys as described in architecture. Run center verification and retention commands on schedules. During database/API failure keep the static general QR/support and 115 visible. Incident response: contain credentials, revoke, preserve redacted technical logs, notify privacy/security owners, recover database, validate general fallback, and only then restore dynamic traffic. Restarted displays begin general mode.
